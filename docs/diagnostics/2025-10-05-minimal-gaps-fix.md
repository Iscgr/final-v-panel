# گزارش رفع گپ‌های مینیمال - 2025-10-05

## هدف
رفع تناقض‌ها و هزینه‌های غیرضروری بدون افزودن فیچر یا تغییر قراردادهای بیرونی.

## موارد انجام‌شده
1. Select پروفایل نماینده: رفع کرش Radix با sentinel value (`__none`).
2. کاهش over-invalidation در ذخیره بلوک‌های پرتال: حذف invalidation کامل (full) مگر در تب preview.
3. همسان‌سازی همین منطق برای اعلان‌ها و دانلودها.
4. افزودن aria-live و role="region" برای وضعیت ذخیره در ویرایشگر بلوک‌ها.
5. افزودن skip-link و landmark اصلی (role=main) در `App.tsx`.
6. افزودن لایه کش ثانویه ۵ ثانیه‌ای در `UnifiedFinancialEngine.calculateRepresentative` برای مهار درخواست‌های تکراری بسیار نزدیک.

## چرایی هر تغییر
| تغییر | مشکل قبلی | راه‌حل | ریسک | Rollback |
|-------|-----------|--------|------|----------|
| Sentinel Select | value خالی → خطای Runtime | مقدار نمادین `__none` و تبدیل در submit | بسیار پایین | حذف خطوط sentinel |
| محدودسازی invalidation بلوک | refetch غیرضروری همه داده‌ها | invalidate فقط blocks (و full در preview) | پایین | بازگرداندن پارامتر full |
| محدودسازی invalidation اعلان/دانلود | الگوی پرهزینه شبیه blocks | همان روش blocks | پایین | بازگرداندن full |
| aria-live / landmark | نبود اعلان تغییر برای SR | ویژگی‌های دسترس‌پذیری بدون تغییر UI | ناچیز | حذف attributes |
| کش ۵ ثانیه مالی | طوفان کوئری در refresh سریع | Soft TTL 5s قبل از TTL اصلی | متوسط (کندی 5s بروزرسانی) | حذف بلاک کش |

## خارج از دامنه (تعمدی انجام نشد)
- Batching محاسبات مالی چند نماینده
- Migration ایندکس‌های پایگاه داده
- تغییر pagination یا طراحی UI

## پیشنهادات بعدی (در صورت نیاز آینده)
1. اضافه کردن instrumentation شمارنده cache-hit.
2. ایندکس‌های ترکیبی برای جداول پرداخت/فاکتور.
3. Batch aggregate واحد برای نمایندگان.

## تایم‌لاین
- 2025-10-05: همه اصلاحات بدون خطای TypeScript اعمال شد.

---
Generated programmatically.
